<div class="grid_1_of_4 images_1_of_4">
  <h4><a href="#"><%= product.name %></a></h4>
  <a href="#">
    <% if product.image? %>
        <%= image_tag product.image.thumb.url %>
    <% else %>
        <img src="http://placehold.it/100x100"
             alt="Alternate Text"/>
    <% end %></a>
  <div class="price-details">
    <div class="price-number">
      <p><span class="rupees"><%= number_to_currency(product.price, unit: '€', format: '%n %u') %> </span></p>
    </div>
    <div class="add-cart">
      <h4><%= link_to t('see_more'), category_product_path(id: product.id, category_id: product.category_id) %></h4>
    </div>
    <div class="clear"></div>
  </div>
</div>
