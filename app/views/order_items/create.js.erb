<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.")
<% else %>
$(".cart-text").html("<%= escape_javascript(render 'layouts/partials/cart_text') %>")
$("#flash-messages").html('<%= j render 'layouts/messages' %>');
<% end %>
