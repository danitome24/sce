<div class="container">
  <div class="row">
    <div class="col-md-12">
      <div id="Carousel" class="carousel slide" data-interval="false">
        <!-- Carousel items -->
        <div class="carousel-inner">
          <div class="item active">
            <div class="row">
              <% @products.take(4).each do |p| %>
                  <%= render 'pages/row_products_slide', product: p %>
              <% end %>
            </div><!--.row-->
          </div><!--.item-->

          <div class="item">
            <div class="row">
              <% @products.take(4).each do |p| %>
                  <%= render 'pages/row_products_slide', product: p %>
              <% end %>
            </div><!--.row-->
          </div><!--.item-->
        </div><!--.carousel-inner-->
        <a data-slide="prev" href="#Carousel" class="left carousel-control">‹</a>
        <a data-slide="next" href="#Carousel" class="right carousel-control">›</a>
      </div><!--.Carousel-->
    </div>
  </div>
</div><!--.container-->